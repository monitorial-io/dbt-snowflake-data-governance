version: 2

macros:
  - name: model_column_meta_contains_items
    description: This macro is designed to determine if a column contains a given meta item
    docs:
      show: false
  - name: model_meta_contains_item
    description: This macro checks if a model's metadata contains a specific item by name
    docs:
      show: false
  - name: get_model_meta_item
    description: Returns the value of a metadata item from config.meta (primary) or top-level config (fallback), or none if not found
    docs:
      show: false