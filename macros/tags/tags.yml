version: 2

macros:
  - name: apply_tags
    description: This macro is designed to be used as post-hook for models to apply the relevant meta data as tags on the view or table
    docs:
      show: true
  - name: set_column_tag_value
    description: This macro is designed to apply a tag to a model if the model contains a column with a given tag meta
    docs:
      show: false